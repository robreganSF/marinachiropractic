# Content index

Every place a business fact appears, so nothing gets missed when something changes.
This site has no build step, so these values are hand-duplicated across pages.

## Current values

| Fact | Value |
| --- | --- |
| Practice name | Marina Chiropractic |
| Doctor | Dr. Kirsti Neely, D.C. |
| Phone / text | 415.563.1046 (`tel:+14155631046`) |
| Email | kirstineely@yahoo.com |
| Scheduling | By appointment only |
| Street address | **None published.** Practice is mobile. |
| Service area | San Francisco and Marin |

## Where each appears

**Phone number** — header CTA and footer of all 6 pages; hero button on `index.html`;
`how-it-works.html` steps 1 and 6 plus the cost and closing sections; `services.html` closing;
`bio.html` closing; `contact.html` contact block; JSON-LD in `index.html`.

**Email** — footer of all 6 pages; `contact.html` contact block; JSON-LD in `index.html`.

**Service area** — footer of all 6 pages; `index.html` body list and JSON-LD `areaServed`;
`contact.html` body list.

**Nav links** — header and footer of all 6 pages. Adding a page means editing 6 files.

## Deliberately absent

- **No street address anywhere.** The Sausalito mailing address is a mail drop, not a
  treatment location. It is used only for the domain registrar and business registration.
  Publishing it would send patients somewhere no one is. Do not add it.
- **No embedded map**, for the same reason.
- **No online form that collects health information.** See README.

## Still needs filling in

- [x] ~~Photos~~ — added. `dr-neely.jpg` on `bio.html`; `van-interior.jpg` and
      `van-exterior.jpg` on `how-it-works.html`; `van-exterior.jpg` also on `index.html`.
      Derived from the originals in `images/` (`chiro-van.jfif`, `van-bridge2.jpg`).
      Unused alternates kept: `van-bridge1.jpg`, `van-bridge-close-up.jpg`.
- [ ] A larger headshot would help — the current one is only 349x341, so it is
      displayed small to stay sharp
- [ ] Visit durations, first visit and follow-up — `how-it-works.html`
- [ ] Cash price, if she decides to publish it — `how-it-works.html`
- [ ] Whether she does home or workplace visits — `how-it-works.html`
- [ ] California DC license number — `bio.html`
- [x] ~~Confirm the intake PDF does not still show the old Chestnut St address~~ — it has no address or contact info at all
